

<nav class="navbar navbar-expand-lg navbar-light nav-color py-3">
  <div class="container d-flex">
         <%= link_to "Back", splashes_path, class: 'text-decoration-none text-light ' %>
   
      <% if controller_name == 'sessions' && action_name == 'new' %>
      <h2 class="text-primary fs-6 text-light">Log In Page</h2>
      <% elsif controller_name == 'registrations' && action_name == 'new' %>
      <h2 class="text-primary fs-6 text-light">Sign Up Page</h2>
      <% elsif controller_name == 'passwords' && action_name == 'new' %>
      <h2 class="text-primary fs-6 text-light">Reset Password Page</h2>
      <% end %>  
    </div>
</nav>


